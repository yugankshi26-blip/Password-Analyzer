import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yugankshi.passwordanalyzer',
  appName: 'Password Analyzer',
  webDir: 'dist/project_new/browser'
};

export default config;
